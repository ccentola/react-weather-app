import React from 'react';

const About = () => (
  <div>
    <h1>About</h1>
    <p>
      Weather search app build on React and Semantic UI with elements of D3.
    </p>
  </div>
);

export default About;
